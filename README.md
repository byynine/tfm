A terminal file manager.

Support only for Unix-based systems.

# Build

Clone the repository and build using Make.  (Run `make`)

# Usage

- Navigation: h, j, k, l -> parent directory, down, up, open directory
- Misc: q, o -> quit, open file
